spring:
  application:
    name: Affaci



  datasource:
    url: jdbc:postgresql://localhost:5432/affaci
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 9876


springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui/index.html